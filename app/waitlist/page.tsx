import WaitlistPage from "./WaitlistPage";

/**
 * Original TapticoAI beta waitlist landing page, preserved at /waitlist.
 * Kept self-contained so its dark-mode brand tokens don't bleed into The GRID.
 */
export default function WaitlistRoute() {
  return <WaitlistPage />;
}
