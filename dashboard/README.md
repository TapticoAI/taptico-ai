# TapticoOS Dashboard

Internal product dashboard. Deployed separately from the marketing site at the root of this repo.

## Run locally

```
cd dashboard
npm install
npm run dev
```

Opens at http://localhost:3001.

## Deploy

Configured in Vercel as a separate project with Root Directory set to `dashboard`.
